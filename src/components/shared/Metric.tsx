import React from "react";

interface MetricProps {
  imgUrl: string;
  alt: string;
  title: string;
  textStyles: string;
}

const Metric = ({ imgUrl, alt, title, textStyles }: MetricProps) => {
  return <div>Metric</div>;
};

export default Metric;
